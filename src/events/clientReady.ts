import { Client } from 'discord.js';

export default (client: Client) => {
    // const MAIN_CHANNEL_ID = '1040034372306346052';
    // client.on('ready', () => {
    //     const channel = client.channels.cache.find(channel => channel.id === MAIN_CHANNEL_ID);
    //     if (channel?.isTextBased()) channel.send('Wstałem');
    // });
};
// console.log();